# debug.md

## 2026-09-13 CDP 端口被 WorkBuddy 占用

### 问题

实时采集环境检查返回 WebSocket 403，提示 9222 端口拒绝连接。

### 原因

9222 端口被 WorkBuddy 进程占用，不是采集器专用 Chrome。

### 解决方式

将专用采集浏览器和采集器改用 9223 端口。

### 如何避免

启动专用浏览器前检查端口占用；项目中统一使用 9223，不占用 9222。

## 2026-09-13 实时采集速度过慢

### 问题

实时采集运行数分钟仍未结束。

### 原因

采集器默认逐条抓取岗位详情页，且详情数量较多。

### 解决方式

改为列表模式，不逐条抓详情；JD 由列表字段回填，降低访问量。

### 如何避免

实时采集只做列表读取；若未来需要详情，应只对少量精选岗位抓取。

## 2026-09-14 DeepSeek 返回 JSON 后带额外文本

### 问题

真实 LLM 返回内容包含 JSON 对象后的额外文本，导致解析报 `Extra data`。

### 原因

模型没有严格只输出一个 JSON 对象。

### 解决方式

使用 `json.JSONDecoder().raw_decode()` 解析第一个完整 JSON 对象，忽略尾随文本。

### 如何避免

保留结构化输出提示，同时解析器必须容忍尾随文本。

## 2026-09-14 DeepSeek 并发请求出现 SSL EOF

### 问题

实时采集完成后，评分阶段出现 `SSL: UNEXPECTED_EOF_WHILE_READING`。

### 原因

并发请求和网络瞬时断连导致 SSL 握手中断。

### 解决方式

增加自动重试，并将并发数降低到 2。

### 如何避免

真实模型请求使用有限并发和重试机制，避免单次网络抖动直接失败。

## 2026-09-14 实时采集报连接失败

### 问题

用户点击实时采集后报 `urllib3` 连接失败。

### 原因

专用浏览器已关闭，9223 端口没有监听。

### 解决方式

重新启动 `setup_browser.bat`，让用户重新登录 BOSS。

### 如何避免

实时采集前检查专用浏览器端口；网页错误提示应明确说明需要重新登录。
