# Claude.md

## 项目背景

用户正在求职产品运营实习。为了提升应聘竞争力，希望制作一个用于简历和面试展示的“求职逆向岗位匹配 Agent”Demo。

与传统“先抓岗位，再匹配简历”的流程不同，本项目先接收目标岗位方向、参考 JD 和简历，提取目标岗位画像与候选人证据，再获取岗位数据并进行匹配评分，最终输出按匹配度排序的岗位推荐清单。

## 项目目标

- 完成一个可运行、可演示的本地产品 Demo。
- 支持固定样例模式和实时只读采集模式。
- 输出岗位名称、公司、城市、薪资、匹配分、核心理由和来源链接。
- 项目只用于展示，不追求上线、真实业务效果或大规模准确率评估。

## 目标用户

- 用户本人，产品运营实习求职者。
- 0 代码基础，主要负责需求、产品判断、账号登录、API Key 配置和最终验收。
- 面试官是最终信息接收者，因此项目要能清晰解释流程、取舍和边界。

## 技术栈

- Python 3.11
- 标准库 HTTP 服务
- HTML、CSS、JavaScript 原生实现
- OpenAI 兼容 LLM 接口，默认 DeepSeek
- eatmoreduck/boss-zhipin-scraper 作为实时采集基础组件
- Markdown 报告

## 开发原则

- 先做能跑通的 Demo，再逐步优化体验。
- 不做前端框架、数据库、部署、账号系统等非必要复杂度。
- 固定样例优先，实时采集作为按需增强。
- 岗位事实只能来自数据源，模型只能负责评分和解释。
- 每次修改保持测试可运行，避免为了效果牺牲稳定性。
- 保持简历和面试描述真实，不把开源组件复用写成独立开发。
- 默认桌面端优先，暂不做移动端适配。

## 项目红线

- 实时采集只读取岗位数据，禁止发送消息、打招呼、投递、收藏或任何写操作。
- 禁止绕过验证码、登录验证或其他访问控制。
- 禁止把模型生成的岗位当成真实岗位；每个岗位必须来自采集结果或本地导入数据。
- 禁止把 API Key、简历、真实岗位数据等敏感信息写入代码或版本控制。
- 不进行高频、持续、商业化的招聘平台访问。
- 不夸大项目效果，不使用无法解释或无法验证的简历表述。
